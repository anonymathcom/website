import nextConfig from '../../../next.config';

export const locales = nextConfig.i18n.locales;
export const defaultLocale = nextConfig.i18n.defaultLocale;
export const flatRoutes = nextConfig.flatRoutes;
